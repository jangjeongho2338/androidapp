import 'package:flutter/material.dart';

class ImportantViewPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('주요 일정 보기')),
      body: Center(child: Text('주요 일정 페이지')),
    );
  }
}