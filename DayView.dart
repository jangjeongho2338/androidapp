import 'package:flutter/material.dart';

class DayViewPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('일별 달력')),
      body: Center(child: Text('일별 보기 페이지')),
    );
  }
}