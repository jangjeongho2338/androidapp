import 'package:flutter/material.dart';

class WeekViewPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('주별 달력')),
      body: Center(child: Text('주별 보기 페이지')),
    );
  }
}